## 1. Resolve the profile and authorization contract

- [ ] 1.1 Add request schema v2 and the execution-mode/quality-profile/review-policy matrix so new autonomous prototype requests and the `prototype` shorthand resolve to `same-session-local`, while legacy independent-review input remains compatible only when consistent. Depends on: none. Evidence: focused resolver fixtures cover explicit requests, both aliases, duration override, legacy compatibility, conflicts, missing inputs, and unchanged production behavior.
- [ ] 1.2 Separate blocking approval gates, required quality actions, and completion-evidence predicates in effective authorization and operation checking without weakening exact target, expiry, runtime permission, or production review gates. Depends on: 1.1. Evidence: authorization fixtures prove autonomous prototype no-routine-pause behavior, required quality/evidence retention, permission denial, and production non-regression.

## 2. Make correction and readiness evidence converge

- [ ] 2.1 Extend the existing correction-chain owner with deterministic canonical signatures, per-signature attempt and stagnation evidence, aggregate-distinct progress, and durable exhausted-signature intervention output. Depends on: 1.2. Evidence: correction fixtures prove more than three aggregate distinct corrections pass, superficial variation does not reset a signature, repetition is stagnation, and a fourth stable-signature attempt is refused.
- [ ] 2.2 Extend implementation-quality and shared result validation so autonomous prototype completion requires every applicable current passing action and final target/package/workspace/head predicate, with failed, missing, stale, mismatched, skipped-required, attempted-only, and unresolved-objective states rejected. Depends on: 1.2, 2.1. Evidence: focused validator fixtures cover passing convergence and every required negative or stop state.

## 3. Deliver the continuous local-review loop

- [ ] 3.1 Update canonical `base-code-review` behavior and result details for a bounded read-only `local-review` worker that cannot mutate, approve, or satisfy production independent-review gates. Depends on: 2.2. Evidence: base-code-review evals validate label, bounded input, ordered findings, objective-fix routing, read-only behavior, and assurance-mismatch rejection.
- [ ] 3.2 Update canonical `base-verification-loop`, autonomous runner, and SDD lifecycle behavior to diagnose, correct, rerun affected checks, and request fresh local review without routine Plan-to-Apply or Verified-to-Close prompts, while preserving material and permission stops. Depends on: 3.1. Evidence: lifecycle fixtures exercise objective correction through final-head convergence plus material-decision, permission, unsafe-action, expiry, and stale-evidence stops.
- [ ] 3.3 Refresh or verify thin Claude/Codex exposure and global packaging from canonical sources without duplicating policy. Depends on: 3.1, 3.2. Evidence: adapter drift, install/list dependency, and second-workspace portability checks pass with no product constants in reusable assets.

## 4. Integrate, document, and verify delivery

- [ ] 4.1 Align repository guidance and the owner-confirmed design brief with the delivered schema, assurance labels, migration rule, correction model, lifecycle reporting, security boundary, and recovery behavior. Depends on: 1.1, 2.1, 3.2. Evidence: documentation and link review finds no contradictory profile behavior, stale field name, unsupported assurance claim, secret, or product constant in canonical assets.
- [ ] 4.2 Run the focused resolver, authorization, correction, validation, review, verification, lifecycle, controller, adapter, and packaging suites; run artifact/tracking validation, security/secret, attribution, portability, recovery, whitespace, and strict OpenSpec validation; map every requirement and scenario in formal Verify. Depends on: 1.1-4.1. Evidence: every required command passes against the final implementation head and the verification report contains no critical or unresolved objective finding.

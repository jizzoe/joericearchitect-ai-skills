# Verification checkpoint

The implementation is checked against the active change's proposal, design,
delta specifications, task list, and tracking record. The validation commands
listed in `requirements-and-review.md` passed against this worktree before the
Apply checkpoint. A strict independent review remains required for the exact
committed head before merge; any head change invalidates this checkpoint and
requires affected checks and review to be repeated.

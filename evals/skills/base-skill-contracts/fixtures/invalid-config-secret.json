{
  "schemaVersion": 1,
  "adapters": { "synthetic-adapter": { "kind": "fixture", "enabled": true, "operations": ["read-source"], "endpoint": "https://example.invalid" } },
  "policies": { "access-token": "synthetic-secret" }
}
